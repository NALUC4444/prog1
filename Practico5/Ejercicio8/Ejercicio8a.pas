(*Letra parte A:
(a) Escriba un programa en Pascal que resuelva lo pedido, suponiendo que no hay espacios
en la entrada.*)
procedure CalcularConEspacios();
var operador, valor : char;
begin
    writeln('Ingrese la cuenta a realizar: ');
    read(valor);
    while valor <> centinela do
    begin
        
    end;
end;